# Copy this file to .env and set your open Meet room.
# cp .env.example .env   (or copy .env.example .env on Windows)

MEET_URL=https://meet.google.com/YOUR-MEET-CODE
BOT_NAME=PC Bot
MODE=chat-only
CHAT_MESSAGE=Hello from the bot
CHAT_INTERVAL_MS=5000
RECORD_SECONDS=60

# false = visible Chrome (recommended on a PC so you can watch join + chat)
HEADLESS=false
WINDOW_SIZE=1280x720

# Local fleets (npm run fleet:10 / fleet:20) — never set CONFIRM_LIVE in CI
# CONFIRM_LIVE=true
# BOT_NAME_PREFIX=PC1
# FLEET_OFFSET=0
# WAVE_SIZE=10
# STARTUP_CONCURRENCY=2
# STARTUP_STAGGER_MS=1500
# WAVE_PAUSE_MS=8000
# RECORD_SECONDS=300
